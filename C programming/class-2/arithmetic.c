#include <stdio.h>

int main()
{
    int a = 30, b = 12;
    // scanf("%d %d", a, b);

    // Summation------------------
    int sum = a + b;
    printf("%d\n", sum);

    // Minus----------------------
    int mis = a - b;
    printf("%d\n", mis);

    // Multiplication-------------
    int mul = a * b;
    printf("%d\n", mul);

    // Division-------------------
    int div = a / b;
    printf("%d\n", div);

    // Division-------------------
    int mod = a % b;
    printf("%d\n", mod);

    return 0;
}